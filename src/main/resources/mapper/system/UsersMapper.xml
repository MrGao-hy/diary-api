<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.system.mapper.UsersMapper">
    <!--id指定接口方法的名称-->
    <!--    <insert id="insert">-->
    <!--        INSERT INTO d_users(id, user_name, password, phone, salt)-->
    <!--        values (#{id}, #{userName}, #{password}, #{phone}, #{salt})-->
    <!--    </insert>-->

    <!--    <update id="update">-->
    <!--        UPDATE d_users-->
    <!--        SET user_name=#{userName},-->
    <!--            password=#{password},-->
    <!--            phone=#{phone},-->
    <!--            avatar=#{avatar},-->
    <!--            birth_date=#{birthDate},-->
    <!--            sex=#{sex},-->
    <!--            age=#{age}-->
    <!--        WHERE id = #{id}-->
    <!--    </update>-->

    <!--    <delete id="deleteById">-->
    <!--        DELETE-->
    <!--        FROM d_users-->
    <!--        WHERE id = #{id}-->
    <!--    </delete>-->

    <!--    <select id="findByUsername" resultType="com.example.demo.system.entity.Users">-->
    <!--        SELECT *-->
    <!--        FROM d_users-->
    <!--        WHERE user_name = #{userName}-->
    <!--    </select>-->

    <!--    <select id="findById" resultType="com.example.demo.system.entity.Users">-->
    <!--        SELECT *-->
    <!--        FROM d_users-->
    <!--        WHERE id = ${id}-->
    <!--    </select>-->
</mapper>
